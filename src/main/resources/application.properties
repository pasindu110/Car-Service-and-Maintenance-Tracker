# ======================================================
#  Car Service and Maintenance Tracker – Application Config
# ======================================================
#
#  This file is read by AppConfig.java or a future Spring Boot context.
#  Add key=value pairs below to configure the application without
#  touching the source code.
#
#  TEAM: Update this file if you add new configurable settings.
# ======================================================

# ── Application ───────────────────────────────────────
app.name=Car Service and Maintenance Tracker
app.version=1.0.0
app.description=OOP student team project

# ── Data Storage ──────────────────────────────────────
data.dir=src/main/resources/data/
data.users.file=users.txt
data.vehicles.file=vehicles.txt
data.services.file=services.txt
data.appointments.file=appointments.txt
data.invoices.file=invoices.txt

# ── Business Rules ────────────────────────────────────
billing.default.tax.rate=0.10
vehicle.max.per.customer=10

# ── Future: Database (uncomment when switching to JDBC) ────
# db.url=jdbc:mysql://localhost:3306/cartracker
# db.username=root
# db.password=yourpassword
# db.driver=com.mysql.cj.jdbc.Driver
