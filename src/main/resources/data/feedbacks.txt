# feedbacks.txt – flat-file storage for Feedback records
# Format: feedbackId|userId|serviceId|rating|comment|createdAt|updatedAt
# One record per line. Lines starting with # are ignored by parseLine().
# Leave this file empty (or with only comments) for a fresh start.